# ===========================================
# VireoAI 环境变量配置
# 复制此文件为 .env 并填入你的配置
# ===========================================

# 数据库 (PostgreSQL)
# 格式: postgresql://用户名:密码@主机:端口/数据库名?schema=public
DATABASE_URL="postgresql://postgres:your-password@localhost:5432/vireoai?schema=public"

# NextAuth 密钥 (用于加密 session)
# 生成方式: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-key-change-in-production"

# 站点 URL (用于邮件中的链接地址)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ===========================================
# AI 服务配置 (至少配置一个)
# ===========================================

# OpenAI API (用于 AI 创意生成和智囊团)
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-your-openai-api-key"

# Claude API (可选，支持 Anthropic Claude)
# 获取地址: https://console.anthropic.com/
# ANTHROPIC_API_KEY="sk-ant-your-anthropic-api-key"

# ===========================================
# 邮件服务配置 (可选，用于邮箱验证)
# ===========================================

# SMTP 配置
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@example.com"
# SMTP_PASS="your-email-password"
# SMTP_FROM="VireoAI <noreply@example.com>"

# ===========================================
# 代理配置 (可选，用于访问 Google Trends 等)
# ===========================================

# 如果你在中国大陆，需要配置代理才能访问 Google Trends
# Clash 默认端口 7890，V2Ray 默认端口 10809
# HTTPS_PROXY="http://127.0.0.1:7890"
# HTTP_PROXY="http://127.0.0.1:7890"
